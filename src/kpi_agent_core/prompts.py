"""
Промпты для каскадирования целей и чата с LLM.

Используются в графе (узлы извлечения, разбивки, проверки) и в API чата.
"""

# —— Извлечение целей из документа руководства ——
CASCADE_EXTRACT_GOALS = """Ты — эксперт по стратегическому планированию. Из текста документа с целями руководства извлеки структурированный список целей.

Для каждой цели укажи:
- title: краткое название цели
- description: описание (1–2 предложения)
- source: из какого раздела/документа (если видно)
- kpi: список связанных KPI или показателей (если упомянуты)

Ответь строго в формате JSON — массив объектов с полями title, description, source, kpi. Без markdown-обёртки и пояснений.
Пример: [{"title": "...", "description": "...", "source": "...", "kpi": []}]

Документ с целями:
{goals_text}
"""

# —— Разбивка целей по подразделениям ——
CASCADE_SPLIT_BY_SUBDIVISION = """По списку целей руководства предложи, как их каскадировать на подразделения: каждую цель привяжи к одному или нескольким подразделениям и сформулируй конкретизированную цель для подразделения.

Исходные цели (JSON):
{goals_json}

Доступные подразделения (если не указаны — предложи типовые: например, Кредитование, Риски, IT, HR): {subdivisions}

Ответь строго в формате JSON — массив объектов:
- subdivision: название подразделения
- goal: объект с полями title, description, source, kpi
- comment: краткий комментарий (опционально)

Пример: [{"subdivision": "Кредитование", "goal": {"title": "...", "description": "...", "source": "...", "kpi": []}, "comment": ""}]
"""

# —— Проверка по чеклистам (стратегия, регламент, бизнес-план) ——
CASCADE_CHECK_CHECKLIST = """Проверь сформулированные цели подразделений на соответствие чеклисту. Чеклист задаёт критерии качества и регламентные требования.

Цели по подразделениям (JSON):
{goals_json}

Текст чеклиста (требования):
{checklist_text}

Для каждой цели подразделения ответь в формате JSON — массив объектов с полями subdivision, goal (как был), checklist_ok (true/false), comment (что скорректировать или «соответствует»).
Сохрани исходную структуру goal; добавь только checklist_ok и comment.
"""

# —— Текст для эмбеддинга (готовый текст чеклиста/документа) ——
# Оборачивает переданный текст типом документа перед вызовом модели эмбеддингов.
EMBED_DOCUMENT_TEMPLATE = """Тип документа: {document_type}.

Содержимое документа:

{content}
"""

# —— Системный промпт для чата (база знаний + каскад) ——
CHAT_SYSTEM_PROMPT = """Ты — помощник по каскадированию KPI и целей организации. Ты опираешься на прикреплённые документы базы знаний: цели руководства, чеклисты стратегии и регламентов, документы подразделений.

Отвечай кратко и по делу. Если пользователь спрашивает о целях или KPI — ссылайся на документы из контекста. При запросе на каскадирование предложи использовать сценарий «Каскад» (API /api/chat/cascade) для получения структурированных целей по подразделениям."""
